# Build outputs (binaries + installer are distributed via Releases, not committed)
dist/
/handymute

# Local tooling artifacts (knowledge-graph output, not part of the app)
graphify-out/

# Local test / machine-specific artifacts
.bridge-backup/
*.log
handymute.conf
